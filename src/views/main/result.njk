{% extends "base.njk" %}

{% block wrappedContent %}

{% from "govuk/components/panel/macro.njk" import govukPanel %}

{% set vatText = "" %}
{% if isVatRegistered %}
    {% set vatText = "You are VAT registered and " + vatAmount + " is VAT." %}
{% else %}
    {% set vatText = "You are not VAT registered" %}
{% endif %}

{{ govukPanel({
  titleText: "Your total is " + total,
  html: vatText
}) }}

{% endblock %}